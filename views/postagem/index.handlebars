<div class="card">
  <div class="card-body">
    <h1>{{postagem.titulo}}</h1>
      <hr>
      <small>Data da publicação: {{postagem.data}}</small>
      <hr>
      <p>{{postagem.conteudo}}</p>
      <a href="/" class="btn btn-primary mt-4" type="submit">Voltar</a>
  </div>
</div>